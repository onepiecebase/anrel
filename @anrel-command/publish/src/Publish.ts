import Command, { CommandInterface } from '@anrel/command'

export default class Publish extends Command implements CommandInterface {
  public async exec(): Promise<void> {
    // nothing todo...
  }
}
