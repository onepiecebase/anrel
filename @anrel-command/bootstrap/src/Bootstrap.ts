import Command, { CommandInterface } from '@anrel/command'

export default class Bootstrap extends Command implements CommandInterface {
  public async exec(): Promise<void> {
    // nothing todo...
  }
}
